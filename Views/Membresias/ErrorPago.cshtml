@{
    ViewData["Title"] = "Error";
}

<div class="container py-5">
    <div class="alert alert-danger">
        <h4>Error al procesar el pago</h4>
        <p>@ViewBag.Error</p>
        <a asp-action="Index" class="btn btn-primary">Volver a intentar</a>
    </div>
</div>