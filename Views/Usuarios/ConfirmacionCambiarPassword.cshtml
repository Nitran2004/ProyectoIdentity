@{
    ViewData["Title"] = TempData["Titulo"]?.ToString() ?? "Confirmación";
}

<h2>@(TempData["Titulo"]?.ToString() ?? "Confirmación")</h2>
<p>@(TempData["Mensaje"]?.ToString() ?? "Proceso realizado correctamente.")</p>
